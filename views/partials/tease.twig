<article class="tease tease-{{ item.post_type }}">

	{% block content %}

	<h2 class="h2"><a href="{{ item.get_path }}">{{ item.title }}</a></h2>
	<p>{{ item.get_preview }}</p>

	{% if item.get_thumbnail %}
		<img src="{{ item.get_thumbnail.get_url }}" />
	{% endif %}

  {% endblock %}

</article>
